<div id = "event-container">
    <h1><%= event.title %></h1>

    <p><%= event.description %></p>

    <p><strong>Location:</strong> <%= event.location %></p>
    <p><strong>Category:</strong> <%= event.category %></p>
    <p><strong>Status:</strong> <%= event.status %></p>
    <p><strong>Organizer:</strong> <%= event.organizerName %></p>

    <p><strong>Start:</strong> <%= event.startDatetime %></p>
    <p><strong>End:</strong> <%= event.endDatetime %></p>

    <p><strong>Attendance:</strong> <%= event.attendeeCount %>/<%= event.capacity ?? "No limit" %></p>

    <button hx-get="/events/<%= event.id %>/edit" hx-target="#event-container" hx-swap="innerHTML">
    Edit
    </button>
</div>

<% if (session.authenticatedUser &&
      (session.authenticatedUser.userId === event.organizerId ||
       session.authenticatedUser.role === "admin")) { %>
  <div id="attendee-list-section">
    <button
      hx-get="/events/<%= event.id %>/attendees"
      hx-target="#attendee-list-section"
      hx-swap="outerHTML"
      class="text-sm text-blue-600 hover:underline mt-4 inline-block cursor-pointer bg-transparent border-none p-0">
      View Attendee List
    </button>
  </div>
<% } %>
